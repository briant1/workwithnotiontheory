.container
  .row
    .col-md-6.col-md-offset-3.sign-up
      %h2 Create an account
      %p
        Upon signup a clock will be started and you will get a requirements (statement of work), wireframes and pdf mockup.  You will have up to 3 days to submit your project.
      %p
        Please understand the difference between perfect and acceptable, don't spend more than 3 days on this.  This should function but not be perfect.  Much like this website...
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        .field
          = f.label :email
          %br/
          = f.email_field :email, autofocus: true, class: 'form-control'
        .field
          = f.label :password
          - if @minimum_password_length
            %em
              (#{@minimum_password_length} characters minimum)
          %br/
          = f.password_field :password, autocomplete: "off", class: 'form-control'
        .field
          = f.label :password_confirmation
          %br/
          = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control'
        %h4
          None of the following is required but the more the merrier :)
        .field
          = f.label :name
          %br/
          = f.text_field :name, class: 'form-control'
        .field
          = f.label :website_url
          .small
            (your personal website)
          = f.text_field :website_url, class: 'form-control'
        .field
          = f.label :github_url
          .small
            (your public github)
          = f.text_field :github_url, class: 'form-control'
        .field
          = f.label :resume_url
          .small
            (your resume, pdf or read only google doc)
          = f.text_field :resume_url, class: 'form-control'
        .field
          = f.label :comments
          .small
            (anything else you want to say)
          = f.text_area :comments, class: 'form-control'
        %br
        .actions
          = f.submit "Begin", class: "form-control btn btn-primary"
